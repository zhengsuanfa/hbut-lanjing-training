# Netlify 配置文件
[build]
  # 构建命令
  command = "npm run build"
  # 发布目录
  publish = "build"

# 重定向规则 - 支持 SPA 路由
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# 环境变量（如果需要）
[build.environment]
  NODE_VERSION = "18"

